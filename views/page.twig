{% extends "base.twig" %}

{% block content %}

{% include 'header.twig' %}
<div class="row">
  <div class="large-12 columns">

  <article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
    <section class="article-content">
      <h1 class="article-h1">{{post.title}}1</h1>

      <div class="article-body">
	{{post.content}}
      </div>
    </section>

  </article>
  </div>
</div>

{% endblock %}
